#include "main.h"

/**
 * print_alphabet_x10 - Check the code
 *
 * Return: Nothing.
 */

void print_alphabet_x10(void)
{
int y = 0;

for (y ; y < 10 : y++)
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
